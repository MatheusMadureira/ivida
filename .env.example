# URL do projeto (frontend e backend)
NEXT_PUBLIC_SUPABASE_URL=https://pqvfjqfydseb...

# 🔓 Chave pública (frontend) — PUBLISHABLE
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxx

# 🔐 Chave privilegiada (backend apenas) — SECRET / SERVICE ROLE
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxxxxxxxx

# Vercel Blob — galeria da home e fotos dos líderes (Ministérios)
# Sem isso, /api/imagens-home e /api/ministry-leaders retornam vazio.
# Vercel: Storage → seu store → .env ou variáveis do projeto
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxx
