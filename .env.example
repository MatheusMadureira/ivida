# URL do projeto (frontend e backend)
NEXT_PUBLIC_SUPABASE_URL=https://pqvfjqfydseb...

# URL base do app (para link de reset de senha). Em produção (ex.: Vercel) defina
# para a URL real (ex.: https://ivida.vercel.app) sem barra no final.
# Se não definido, usa o origin da requisição (pode falhar e mandar o link para /).
# NEXT_PUBLIC_APP_URL=https://ivida.vercel.app

# 🔓 Chave pública (frontend) — PUBLISHABLE
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxx

# 🔐 Chave privilegiada (backend apenas) — SECRET / SERVICE ROLE
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxxxxxxxx

# Sessão JWT (obrigatório em produção). Em dev pode ficar ausente (usa fallback).
# JWT_SECRET=seu_secret_longo_e_aleatorio

# Hash de CPF (obrigatório em produção se usar fluxo por código de restauração). Em dev pode ficar ausente.
# CPF_HASH_SECRET=outro_secret_dedicado

# Vercel Blob — galeria da home e fotos dos líderes (Ministérios)
# Sem isso, /api/imagens-home e /api/ministry-leaders retornam vazio.
# Necessário também para upload de avatar (sem token, a API retorna 503).
# Vercel: Storage → seu store → .env ou variáveis do projeto
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxx

# Logout por inatividade (opcional): tempo em ms.
# Se estiver definida: usa esse valor (ex.: 10000 = 10s para testar).
# Se estiver comentada/ausente: sempre 10 minutos, inclusive em dev.
# NEXT_PUBLIC_INACTIVITY_MS=10000
